# 验证记录 · v1.5 · 2026-09-18

## 已通过

- **93 / 93 自动回归**：保留此前 81 项移动、瞄准取消、近战、改键、Shift 长短按、枪械、机器人和骨架测试；新增 12 项起停时序、瞄准制动、斜向加速度一致性、空中转向动量、翻滚冷却/落地缓冲、短按方向保持、缓冲超时与取消、提前蹲跳、即时视角避障、即时枪口与有效射击采样测试。
- **9 / 9 浏览器响应验收**：真实 DOM 输入与渲染循环验证起步、急停、反向移动、带方向的提前翻滚、着地翻滚、90° / 180° 转视角同帧射击、暂停清除缓冲及三个独立调节滑杆。首次运行发现方向键与翻滚键在同一物理帧前都释放时会丢失方向；修复输入采样后完整重跑通过。
- 平地默认数值的自动测量：静止到 12.5 m/s 奔跑 **150.0 ms**；松键到静止 **116.7 ms / 0.627 m**；反向输入 200 ms 后沿新方向达到 **9.87 m/s**。只代表本 demo 的默认测试场景，不是 Warframe 官方数据或输入设备端到端延迟。
- **57 / 57 浏览器跑酷与近战回归**：真实 WebGL、DOM 键鼠事件和固定步长循环下完整通过。沿用 v1.3 的动作链，更新 E 近战、Z 切棍剑和 Backspace 复位的输入。
- **26 / 26 浏览器战斗验收**：三枪及三模式菜单；Shift 长按后松开保持、再次长按关闭、短按翻滚；实际准星命中；HUD 弹药；完整换弹；手枪半自动；数字键、F 和滚轮切枪；独立弹匣和换弹中断；近战回到瞄准；子弹跳接射击；射击改键保存、新旧按键行为；机器人索敌移动；倒地提示与自动重生；暂停清除持续输入。
- 近景检查了曲面头盔、胸甲及背面奔跑轮廓，真实场景中查看了新背甲与支撑脚。保留全部脚部接地、近战双手握点、三把枪瞄准/换弹和极端朝向的自动测试。模型和全部挂载武器共约 3.7 万三角面，按关节/材质合并静态饰件；这不是实测帧率保证。
- 本轮界面沿用 v1.4 布局；v1.4 曾在 1280 × 720 与 696 × 805 下验证 HUD 不重叠。本轮增加的三项响应滑杆使用原有可滚动设置面板。
- 响应与完整跑酷验收页面控制台未发现 warn/error。一次浏览器/本机服务中断后恢复服务，完整跑酷验收重新运行至 PASS；未将中断的运行计为完整通过。
- `npm run build` 成功。最终 JS **643.47 kB / gzip 178.93 kB**，CSS **19.76 kB / gzip 5.11 kB**。Vite 保留单包超过 500 kB 的体积提示，资源全部本地提供；生产包不含开发验收钩子。
- 独立服务 `/health` 返回 `1.5.0`，HTML 显示 `01.5`，JS/CSS 返回 200 和正确内容类型。启动脚本沿用此前已验证版本。

## 复现

```powershell
npm test
npm run dev
```

打开 <http://127.0.0.1:8770/?test=1>，点击「运行浏览器验收」，成功显示 `PASS 57 / 57`。

打开 <http://127.0.0.1:8770/?test=combat>，点击「运行战斗验收」，成功显示 `PASS 26 / 26`。测试会布置临时目标，并在结束后恢复原按键和模式偏好。后台页面节流可能使测试持续数分钟，期间不要切走或暂停。

打开 <http://127.0.0.1:8770/?test=response>，点击「运行响应验收」，成功显示 `PASS 9 / 9`。转向射击用确定的左右目标布置，发射仍经过真实输入事件、物理更新、当前镜头与枪口遮挡采样。

姿态检查入口：`/tests/combat-review.html`（持枪、换弹、奔跑、背甲）、`/tests/melee-review.html`（棍剑）和 `/tests/animation-review.html`（跑酷）。这些入口仅通过开发服务访问。

## 范围与边界

验证浏览器为 Codex 内嵌 Chromium，其 Pointer Lock 不可用，实际验证的是无需按左键的鼠标跟随回退。完整无限转向使用标准 Pointer Lock，独立 Chrome / Edge 尚未进行长时间人工试玩。没有宣称实测帧率，也未验证手柄或触屏。

模型和动作使用原创程序几何与关节动画，枪械数值和机器人行为为训练参数，不是原作逐帧标定结果。机器人只在主广场的平面网格上导航，不会追上高台或跑酷；战斗尚无完整伤害类型、Mod、处决或联网。

回归验证规则与输入是否正确，动作的自然程度仍需通过试玩继续判断。
